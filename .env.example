# Copy this file to .env and fill only the values needed by your config.

# Required only when notifications.email_enabled=true.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
NOTIFICATION_EMAIL=

# Required only when ai.enabled=true and the OpenAI classifier is used.
OPENAI_API_KEY=
